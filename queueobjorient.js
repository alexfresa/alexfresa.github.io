var q = new Queue;

var q = function () {
    this.array = [4, 10, 12, 40, 51];
    var a = 0;
}

    q.prototype.isEmpty = function () {
    return 0 == array.length;
    }

    q.protoype.size = function () {
    return array.length;
    }

    q.prototype.front = function () {
    return array(0);
    }

    q.protoype.enqueue = function () {
    q.enqueue("1");
    return this.array;
    }

    q.protoype.dequeue = function () {
    return this.array.shift();
    }





